version: '1.0'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./app:/usr/share/nginx/html
    networks:
      - webnet

networks:
  webnet: